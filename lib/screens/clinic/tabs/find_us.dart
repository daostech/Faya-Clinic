import 'package:flutter/material.dart';

class ClinicFindUsTab extends StatelessWidget {
  const ClinicFindUsTab({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("ClinicFindUsTab"),
      ),
    );
  }
}
